{
  "name": "@fcs/backend",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "//start": "tsx, not node --experimental-strip-types: node's ESM resolver needs an explicit extension on every relative import, and this codebase imports './server' rather than './server.ts'. `npm start` failed on the first import. A production deployment should compile ahead of time rather than rely on a dev runner.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down",
    "db:apply": "node db/apply.mjs",
    "db:smoke": "tsx db/smoke.ts",
    "db:reset": "docker compose down -v && docker compose up -d --wait && node db/apply.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "@fcs/shared": "*",
    "fastify": "^5.12.0",
    "firebase-admin": "^14.2.0",
    "ioredis": "^6.0.0",
    "pg": "^8.23.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/pg": "^8.21.0",
    "tsx": "^4.19.0"
  }
}
